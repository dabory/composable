{{-- 자주쓰는 메세지 hidden --}}

<div id="required-item-omitted" hidden>{{ _e('(*)Required item(s) omitted') }}</div>
<div id="action-failed" hidden>{{ _e('Action failed') }}</div>
<div id="can-not-delete-in-the-status" hidden>{{ _e('Can NOT delete in the status') }}</div>
<div id="comfirm-delete" hidden>{{ _e('Comfirm delete') }}</div>
<div id="can-not-recover-after-delete" hidden>{{ _e('Can NOT recover after delete') }}</div>
<div id="delete" hidden>{{ _e('Delete') }}</div>
<div id="cancel" hidden>{{ _e('Cancel') }}</div>
<div id="can-not-copy-in-the-status" hidden>{{ _e('Can NOT copy in the status') }}</div>
<div id="can-not-paste-in-the-status" hidden>{{ _e('Can NOT paste in the status') }}</div>
<div id="no-data-found" hidden>{{ _e('No data found') }}</div>

<div id="action-completed" hidden>{{ _e('Action completed') }}</div>
<div id="api-request-failed-please-check" hidden>{{ _e('API request failed. Please Check') }}</div>
<div id="the-token-does-not-exist" hidden>{{ _e('The token does not exist') }}</div>
<div id="finish-editting-in-the-last-item-line" hidden>{{ _e('Finish editting in the last item line') }}</div>

<div id="can-not-move-up-or-down-in-the-status" hidden>{{ _e('Can NOT move UP or DOWN in the status') }}</div>
<div id="click-the-checkbox-es-of-line-for-action" hidden>{{ _e('Click the checkbox(es) of line for action') }}</div>
<div id="last-item-line-is-removed" hidden>{{ _e('Last item line is removed') }}</div>
<div id="please-enter-a-number" hidden>{{ _e('Please enter a number') }}</div>

